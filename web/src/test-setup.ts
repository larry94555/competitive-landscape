// Adds the DOM matchers — toBeInTheDocument, toBeDisabled, toHaveTextContent — which read
// far better in a failure message than a bare boolean assertion would.
import "@testing-library/jest-dom/vitest";
