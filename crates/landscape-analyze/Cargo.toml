[package]
name = "landscape-analyze"
# Not published; see landscape-golden's note about path dependencies and cargo-deny.
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "One subject in, one report out: discovery, extraction, and what was checked."

[dependencies]
landscape-core.workspace = true
landscape-discover.workspace = true
landscape-extract.workspace = true
landscape-fetch.workspace = true
landscape-llm.workspace = true
landscape-search.workspace = true
tracing.workspace = true
chrono.workspace = true

[dev-dependencies]
# For the canned `SourceProvider` the search-pass tests stand up.
async-trait.workspace = true
# A stub `llama-server`, so the one thing worth asserting about cancellation — that the model
# stops being called — can be asserted without a GPU.
axum.workspace = true
landscape-golden.workspace = true
serde_json.workspace = true
tokio.workspace = true

[lints]
workspace = true
